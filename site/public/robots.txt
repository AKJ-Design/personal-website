# Flipped 2026-09-02, build step 8 — the site is served from its real domain and
# the workers.dev preview host is retired (`workers_dev: false` in wrangler.jsonc),
# so this file can no longer be read from a second host carrying the same person.
#
# That retirement is the reason this could be opened: before it, both hosts served
# identical HTML including this file, and "Alex Young" is a common name — a
# workers.dev URL competing with alexyoung.com.au for the same entity is precisely
# what design plan §8's entity-consistency work exists to prevent.

User-agent: *
Allow: /

Sitemap: https://alexyoung.com.au/sitemap-index.xml
